import { Actions } from "./reducer";
import * as API from '../../services/moviesService'

const getMovies = () => {
    return async(dispatch) => {
        try {
            dispatch(Action)
        }
    }
}