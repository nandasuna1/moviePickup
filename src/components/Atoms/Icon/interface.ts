export interface IconInterface {
    name?: string;
    // size?: 'small' | 'normal' | 'big';
    size?: number;
    // position?: 'center' | 'left' | 'right';
    color?: string;
    isBack?: boolean;
}